# APP 项目简明交接

日期：2026-07-11  
仓库：`x0815fm-cpu/ai-ip-content-app`  
唯一事实来源：GitHub `main`

## 1. 完整上下文

不要把长篇历史复制进新对话框。

完整产品背景、已完成 PR、当前问题、推进计划、验收规则和协作方式统一维护在：

```text
docs/product/app-project-context.md
```

新对话框先读取该文件，再核对 `main` 当前代码；文档与代码冲突时，以代码和最新 Review 为准。

## 2. 当前状态

已完成：

- PR #7：真实失败输入步骤。
- PR #8：真实失败参与选题推荐，空故事不再虚构经历。
- `ContentScreen` 已有基础防白屏保护。

尚未完成：

- `generate_content` 正式 Prompt。
- `generate_content` 服务端返回结构校验。
- 真实模型从首页到文案页的完整闭环验收。

## 3. 下一步唯一目标

```text
First Content Experience V1
```

用户必须能够体验：

```text
首页
→ 三道题
→ 方向
→ 真实失败或跳过
→ 选题
→ 生成中状态
→ 正常看到真实文案
```

主流程未完整走通前，不推进故事库、长期记忆、30 天规划或复杂 Agent。

## 4. 新对话框启动动作

先核对：

1. `generate_content` 是否仍使用 `DEFAULT_PROMPT`。
2. Provider 是否已经校验 `generate_content` 返回结构。
3. 是否存在 PR-09 技术方案或功能分支。
4. 当前网页从首页走到文案页的真实表现。

然后只输出：

- 当前真实状态
- 下一步唯一任务
- 完成后用户能体验到什么
- 一键复制给 Codex 的话术

## 5. 固定原则

> 每一轮开发，都必须让用户在网页上看得见、点得到、感受得到。

每次给 Codex 指令时，自动附上一键复制话术，不需要用户再次提醒。
